# neural-amp-modeler-wasm declares react ^18 as a peer, but we only import its
# framework-agnostic `/engine` subpath, never its React components. Next 15
# needs React 19, so let npm resolve past that peer conflict instead of
# pinning the whole app to React 18 for a dependency we don't render.
legacy-peer-deps=true
